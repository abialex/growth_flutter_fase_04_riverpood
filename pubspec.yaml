name: app_ui_kit
description: "Reusable Flutter design system: tokens, atoms, molecules and organisms."
version: 0.3.0
publish_to: 'none'

environment:
  sdk: ^3.9.2
  flutter: ">=3.35.0"

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^6.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  very_good_analysis: ^10.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/fonts/Inter/
