name: growth_design_system_workspace
description: Monorepo workspace for the reusable Flutter design system and its showcase app.
publish_to: "none"

environment:
  sdk: ">=3.0.0 <4.0.0"

dev_dependencies:
  melos: ^6.0.0
  flutter_lints: ^5.0.0
