/// Visual values that belong to the button component contract rather
/// than to the shared design scales.
class AppButtonTokens {
  const AppButtonTokens._();

  /// Minimum tappable height required by accessibility guidelines.
  static const double minTouchTarget = 44;

  /// Visible height of the compact button variant.
  static const double smallHeight = 36;

  /// Stroke width of the loading indicator inside a button.
  static const double loadingIndicatorStrokeWidth = 2;
}
