/// [assist] is a plain, informational/tap-to-act tag (the "Tag" mockup).
/// [filter] is selectable on/off — pass [onSelected] and [isSelected].
/// [input] shows a trailing delete icon — pass [onDeleted].
enum AppChipType {
  assist,
  filter,
  input,
}
